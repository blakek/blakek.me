import styled from 'styled-components';

const Content = styled.section`
  margin: 0.8em 0;
  max-width: 800px;
  text-align: left;
  width: 100%;

  & > a {
    color: #7142ff;
  }
`;

export default Content;
